---
const base = import.meta.env.BASE_URL;
const withBase = (path: string) => `${base}${path.replace(/^\//, '')}`;

const links = [
	{ href: withBase('/'), label: 'Home', icon: 'fa-house' },
	{ href: withBase('/about'), label: 'About', icon: 'fa-user' },
	{ href: withBase('/research'), label: 'Research', icon: 'fa-microscope' },
	{ href: withBase('/media'), label: 'Media', icon: 'fa-newspaper' },
	{ href: withBase('/teaching'), label: 'Teaching', icon: 'fa-chalkboard-teacher' }
];
---

<header class="nav-shell">
	<div class="brand">
		<a href={withBase('/')} class="brand-link">
			<img src={withBase('/favicon.svg')} alt="Nim Dvir logo" class="brand-icon" />
			<span>Nim Dvir</span>
		</a>
	</div>
	<input type="checkbox" id="nav-toggle" class="nav-toggle" aria-label="Toggle navigation" />
	<label for="nav-toggle" class="menu-toggle" aria-hidden="true">
		<span></span>
		<span></span>
		<span></span>
	</label>
	<nav>
		<ul>
			{links.map((link) => (
				<li>
					<a href={link.href}><i class={`fas ${link.icon}`}></i> {link.label}</a>
				</li>
			))}
		</ul>
	</nav>
</header>

<script>
	// Add shadow on scroll
	document.addEventListener('DOMContentLoaded', () => {
		const nav = document.querySelector('.nav-shell');
		if (nav) {
			window.addEventListener('scroll', () => {
				if (window.scrollY > 10) {
					nav.classList.add('scrolled');
				} else {
					nav.classList.remove('scrolled');
				}
			});
		}
	});
</script>

<style>
	.nav-shell {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 16px 24px;
		border-bottom: 1px solid rgba(0, 0, 0, 0.06);
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		background: rgba(255, 255, 255, 0.85);
		z-index: 1000;
		transition: box-shadow 0.3s ease, background 0.3s ease, padding 0.3s ease;
	}

	.nav-shell.scrolled {
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
		background: #0f172a;
		padding: 12px 24px;
	}

	.nav-shell.scrolled .brand-link {
		color: #fff;
	}

	.nav-shell.scrolled nav a {
		color: rgba(255, 255, 255, 0.9);
	}

	.nav-shell.scrolled nav a:hover {
		background: #fff;
		color: #0f172a;
	}

	.nav-shell.scrolled .menu-toggle {
		background: transparent;
		border-color: rgba(255, 255, 255, 0.3);
	}

	.nav-shell.scrolled .menu-toggle span {
		background: #fff;
	}

	.brand-link {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		font-weight: 700;
		font-size: 18px;
		text-decoration: none;
		color: #0f172a;
		letter-spacing: -0.01em;
	}

	.brand-icon {
		height: 22px;
		width: 22px;
		border-radius: 6px;
		border: 1px solid rgba(0, 0, 0, 0.08);
		object-fit: cover;
		background: #fff;
	}

	nav ul {
		list-style: none;
		display: flex;
		gap: 16px;
		margin: 0;
		padding: 0;
	}

	nav a {
		font-weight: 600;
		text-decoration: none;
		color: #1f2937;
		padding: 8px 10px;
		border-radius: 8px;
		transition: background 0.2s ease, color 0.2s ease;
	}

	nav a:hover {
		background: #0f172a;
		color: #f8fafc;
	}

	.nav-toggle {
		display: none;
	}

	.menu-toggle {
		display: none;
		flex-direction: column;
		justify-content: center;
		gap: 6px;
		width: 42px;
		height: 42px;
		border-radius: 10px;
		border: 1px solid #e5e7eb;
		padding: 10px;
		cursor: pointer;
		background: #f8fafc;
	}

	.menu-toggle span {
		display: block;
		height: 2px;
		background: #0f172a;
		border-radius: 999px;
		transition: transform 0.2s ease, opacity 0.2s ease;
	}

	@media (max-width: 800px) {
		.nav-shell {
			flex-wrap: wrap;
			gap: 12px;
			background: rgba(255, 255, 255, 0.98);
		}

		.nav-shell.scrolled {
			/* Keep dark background from main styles */
		}

		.menu-toggle {
			display: inline-flex;
		}

		nav {
			width: 100%;
			display: none;
			background: #ffffff;
			margin: 0 -24px;
			padding: 0 24px;
			border-top: 1px solid #e5e7eb;
		}

		nav ul {
			display: flex;
			flex-direction: column;
			width: 100%;
			gap: 0;
		}

		nav li {
			border-bottom: 1px solid #f1f5f9;
		}

		nav li:last-child {
			border-bottom: none;
		}

		nav a {
			display: block;
			padding: 14px 10px;
			border-radius: 0;
		}

		.nav-toggle:checked ~ nav {
			display: block;
		}

		.nav-toggle:checked + .menu-toggle span:nth-child(1) {
			transform: translateY(8px) rotate(45deg);
		}

		.nav-toggle:checked + .menu-toggle span:nth-child(2) {
			opacity: 0;
		}

		.nav-toggle:checked + .menu-toggle span:nth-child(3) {
			transform: translateY(-8px) rotate(-45deg);
		}
	}

	@media (max-width: 640px) {
		.nav-shell {
			align-items: center;
			padding: 14px 18px;
		}

		.nav-shell.scrolled {
			padding: 10px 18px;
		}

		nav {
			margin: 0 -18px;
			padding: 0 18px;
		}
	}
</style>

